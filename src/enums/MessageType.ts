export enum MessageType {
	BASE = 1,
	MESSAGE,
	BROADCAST,
	ROOM_REQUEST_JOIN,
	ROOM_REQUEST_LEAVE,
	ROOM_REQUEST_CREATE,
	ROOM_JOINED,
	ROOM_LEFT,
	ROOM_PRESENCE,
	ROOM_CHANGE,
	ROOM_CLOSING,
	CHAT_MESSAGE,
	ADMIN_JOIN,
	ADMIN_COMMAND,
}
